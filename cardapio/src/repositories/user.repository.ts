/**
 * TODO: UserRepository — acesso ao banco de dados de usuários.
 * Consultas à tabela user e campos de domínio (role/status).
 */

export class UserRepository {
  // TODO: findAll, findById, findByUsername, update, setStatus, etc.
}

/**
 * TODO: CategoryRepository — acesso ao banco de dados de categorias.
 * Única camada que deve usar Drizzle para a entidade Category.
 */

export class CategoryRepository {
  // TODO: findAll, findById, create, update, delete, etc.
}

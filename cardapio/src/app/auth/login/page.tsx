import styles from "./page.module.css";

/**
 * TODO: Página de login da área administrativa.
 * Integrar com Better Auth (email/senha).
 */
export default function LoginPage() {
  return <main className={styles.main}>{null}</main>;
}

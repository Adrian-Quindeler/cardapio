import styles from "./AdminHeader.module.css";

/**
 * TODO: Header do painel administrativo (usuário logado, logout, etc.).
 */
export function AdminHeader() {
  return <header className={styles.root}>{null}</header>;
}

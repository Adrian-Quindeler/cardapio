import styles from "./Modal.module.css";

/**
 * TODO: Componente Modal reutilizável.
 * Futuro: detalhes do produto e formulários admin.
 * Bootstrap poderá ser usado pontualmente aqui.
 */
export function Modal() {
  return <div className={styles.root}>{null}</div>;
}
